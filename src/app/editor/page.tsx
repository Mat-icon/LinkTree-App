import React from "react";
import Container from "../(components)/Container";

const page = () => {
  return (
    <div className="instrument-sans">
      <Container />
    </div>
  );
};

export default page;
